# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build api-crawler`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t api-crawler`.
FROM docker.io/node:lts-alpine

ENV HOST
ENV PORT

WORKDIR /app

RUN addgroup --system api-crawler && \
          adduser --system -G api-crawler api-crawler

COPY dist/apps/api-crawler api-crawler
RUN chown -R api-crawler:api-crawler .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix api-crawler --omit=dev -f install

CMD [ "node", "api-crawler" ]
